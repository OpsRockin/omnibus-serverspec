---
general:
  artifacts:
    - "pkg"
machine:
  ruby:
    version: 2.1.5
  pre:
    - echo 'DOCKER_OPTS="-s btrfs -e lxc -D --userland-proxy=false"' | sudo tee -a /etc/default/docker
    - sudo curl -L -o /usr/bin/docker 'https://s3-external-1.amazonaws.com/circle-downloads/docker-1.8.2-circleci'
    - sudo chmod 0755 /usr/bin/docker
  services:
    - docker
dependencies:
  pre:
    #     - wget https://dl.bintray.com/mitchellh/vagrant/vagrant_1.7.4_x86_64.deb
    #     - sudo dpkg -i vagrant_1.7.4_x86_64.deb
    #     - sudo mkdir -p ~/.vagrant.d/
    #     - if [ ! -d /home/ubuntu/.vagrant.d/gems/gems/vagrant-vbguest-0.10.0 ] ; then sudo vagrant plugin install vagrant-vbguest ; fi
    #     - if [ ! -d /home/ubuntu/.vagrant.d/gems/gems/vagrant-digitalocean-0.7.7 ] ; then sudo vagrant plugin install vagrant-digitalocean ; fi
test:
  pre:
    - docker run --rm -it -e OMNIBUS_PROJECT=serverspec opsrock/omnibus_base_ubuntu14:
      timeout: 1200
#   override:
#     - "ci/circle_build_parallel.sh":
#         timeout: 1200
#         parallel: true
#     - "bundle exec rake sync":
#         parallel: true
#     - "ci/circle_release.sh":
#         parallel: true
